name: lint
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: install
        run: make install
      - name: build
        run: make build
      - name: run test
        run: make test
